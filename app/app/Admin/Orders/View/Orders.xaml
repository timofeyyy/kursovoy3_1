<UserControl x:Class=" app.app.Admin.Orders.View.Orders"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:app.app.Admin.Orders.View"
             xmlns:utils="clr-namespace:app.utils"
             xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <utils:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="0.2*"/>
                <RowDefinition Height="11*"/>
                <RowDefinition Height="0.5*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.75*"/>
                <ColumnDefinition Width="10*"/>
                <ColumnDefinition Width="1.75*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="Orders" Style="{StaticResource EntryTB}" HorizontalAlignment="Left"/>

            
            <Grid Grid.Row="2" Grid.Column="1">
                <Border CornerRadius="20" Margin="0,20,0,0"  Background="White"  BorderThickness="0" BorderBrush="Transparent">
                    <StackPanel Orientation="Vertical">
                        <Border Background="#ED9D2E" Height="80" CornerRadius="20" BorderThickness="0" BorderBrush="Transparent">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="Id" Style="{StaticResource ParamTB}" />
                                    <TextBlock Text="Login" Style="{StaticResource ParamTB}" Margin="50,0,0,0"/>
                                    <TextBlock Text="Product id" Style="{StaticResource ParamTB}" Margin="50,0,0,0"/>
                                </StackPanel>
                                <StackPanel Grid.Row="1" Background="White"/>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>
                <Border Margin="0,60,0,0">
                    <DataGrid 
                     ItemsSource="{Binding Orders}"
                     AutoGenerateColumns="False"
                     Background="Transparent"
                     GridLinesVisibility="None" 
                     HorizontalAlignment="Stretch"
                     HeadersVisibility="Column"
                     HorizontalGridLinesBrush="Transparent" 
                     VerticalGridLinesBrush="Transparent"
                     BorderThickness="0"
                     CanUserAddRows="False"
                     >
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="Padding" Value="5" />
                            </Style>
                        </DataGrid.CellStyle>
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="Foreground" Value="Black" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Center" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn  >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Width="70" >
                                            <TextBlock Background="Transparent" Text="{Binding Id}" Style="{StaticResource ParamTB}" Foreground="Black" VerticalAlignment="Center" Margin="15,0,0,0" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Width="100" HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding User.Login}" Style="{StaticResource ParamTB}" Foreground="Black" Margin="10,0,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Width="100" HorizontalAlignment="Center">
                                            <TextBlock Margin="0" Text="{Binding Laptop.Id}" Visibility="{Binding LaptopId, Converter={StaticResource NullToVisibilityConverter}}" Style="{StaticResource ParamTB}" Foreground="#272B2F"/>
                                            <TextBlock Margin="0" Text="{Binding Headphones.Id}" Visibility="{Binding HeadphonesId, Converter={StaticResource NullToVisibilityConverter}}" Style="{StaticResource ParamTB}" Foreground="#272B2F"/>
                                            <TextBlock Margin="0" Text="{Binding Phone.Id}" Visibility="{Binding PhonesId, Converter={StaticResource NullToVisibilityConverter}}" Style="{StaticResource ParamTB}" Foreground="#272B2F"/>
                                            <TextBlock Margin="0" Text="{Binding SmartWatch.Id}" Visibility="{Binding SmartWatchesId, Converter={StaticResource NullToVisibilityConverter}}" Style="{StaticResource ParamTB}" Foreground="#272B2F"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <DockPanel  HorizontalAlignment="Right" Width="220"  >
                                            <Button PreviewMouseDown="ViewStatus" HorizontalAlignment="Right" Focusable="False" Style="{StaticResource NoHoverButtonStyle}" Width="15" Height="15" Margin="5,0" >
                                                <Image Source="{svg:SvgImage ../../../../../../res/icons/edit.svg}"/>
                                            </Button>
                                        </DockPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
