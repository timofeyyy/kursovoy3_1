<UserControl x:Class="app.app.Admin.Products.Phone.Add.First.View.First"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
             xmlns:local="clr-namespace:app.app.Admin.Products.Phone.Add.First.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Style="{StaticResource Page_Style}">
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="12*"/>
            <RowDefinition Height="0.3*"/>
            <RowDefinition Height="1.2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="15*"/>
            <ColumnDefinition Width="2.5*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="1" Text="Images" Style="{StaticResource EntryTB}" HorizontalAlignment="Left"/>

        <Grid Grid.Row="2" Grid.Column="1">

            <Grid Width="530" Height="340"  HorizontalAlignment="Left" VerticalAlignment="Top">

                <Border Background="Gray" CornerRadius="20"  Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
                    <Border.Clip>
                        <RectangleGeometry Rect="0,0,530,340" RadiusX="10" RadiusY="10"/>
                    </Border.Clip>
                    <Image Source="{Binding CurrentImage}" Stretch="UniformToFill"/>
                </Border>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.2*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="0.2*"/>
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Row="0" Grid.Column="1">
                        <Image Source="{svg:SvgImage ../../../../../../../res/icons/left.svg}" Height="20" Width="20" />
                        <Button Background="Transparent" Command="{Binding PrevImageCommand}" Style="{StaticResource NoHoverButtonStyle}" Cursor="Hand"/>
                    </Grid>
                    <Grid Grid.Row="2" Grid.Column="3">
                        <Image Source="{svg:SvgImage ../../../../../../../res/icons/right.svg}" Height="20" Width="20" />
                        <Button Background="Transparent" Command="{Binding NextImageCommand}" Style="{StaticResource NoHoverButtonStyle}" Cursor="Hand"/>
                    </Grid>
                </Grid>
            </Grid>

        </Grid>

        <DockPanel Grid.Row="4"  Grid.Column="1" >
            <StackPanel Orientation="Horizontal" >
                <Border  Background="#ED9D2E" CornerRadius="5" Width="70"  HorizontalAlignment="Left" >
                    <Button Content="Add" Style="{StaticResource NoHoverButtonStyle}" Command="{Binding AddImageCommand}" Margin="0,10" Cursor="Hand" />
                </Border>
                <Border Background="#ED9D2E" CornerRadius="5" Width="70"  Margin="20,0" HorizontalAlignment="Left">
                    <Button Content="Remove" Style="{StaticResource NoHoverButtonStyle}" Command="{Binding RemoveImageCommand}" Cursor="Hand" />
                </Border>
            </StackPanel>
            <Border   Background="#ED9D2E" CornerRadius="5" Width="70"  HorizontalAlignment="Right">
                <Button Content="Next" Style="{StaticResource NoHoverButtonStyle}" Command="{Binding NextCommand}" Margin="0,10" Cursor="Hand" />
            </Border>
        </DockPanel>
        
    </Grid>
</UserControl>
