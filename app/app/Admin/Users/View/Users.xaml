<UserControl x:Class="app.app.Admin.Users.View.Users"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
             xmlns:local="clr-namespace:app.app.Admin.Users.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="0.2*"/>
                <RowDefinition Height="11*"/>
                <RowDefinition Height="0.5*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.75*"/>
                <ColumnDefinition Width="10*"/>
                <ColumnDefinition Width="1.75*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="Users" Style="{StaticResource EntryTB}" HorizontalAlignment="Left"/>

            <Grid Grid.Row="1" Grid.Column="1">
                <DockPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Border BorderThickness="{Binding ClientHighlight}" BorderBrush="White" Margin="0,0,10,0" >
                            <Button Content="Clients" Padding="10,0,10,0" Style="{StaticResource NoHoverButtonStyle}" Command="{Binding SelectClientUsersCommand}" />
                        </Border>
                        <Border BorderThickness="{Binding AdminHighlight}" BorderBrush="White" >
                            <Button Content="Admins" Padding="10,0,10,0"  Style="{StaticResource NoHoverButtonStyle}" Command="{Binding SetectAdminUsersCommand}" />
                        </Border>
                    </StackPanel>
                    <Border Style="{StaticResource ParamBorder}" HorizontalAlignment="Right" VerticalAlignment="Top" Width="120" Margin="0,3,0,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBox Width="75"  Style="{StaticResource ParamBorderTB}" Margin="15, 5, 0, 5" Text="{Binding SearchValue}" />
                            <Grid Width="15" Height="15">
                                <Border>
                                    <Image Source="{svg:SvgImage ../../../../res/icons/magnifier.svg}"  Stretch="Uniform" />
                                </Border>
                                <Border Width="15" Height="15">
                                    <Button Style="{StaticResource NoHoverButtonStyle}" Cursor="Hand" Width="15" Height="15" Margin="0" Command="{Binding FindCommand}"/>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Border>
                </DockPanel>
            </Grid>
            <Grid Grid.Row="3" Grid.Column="1">
                <Border CornerRadius="20" Margin="0,20,0,0"  Background="White"  BorderThickness="0" BorderBrush="Transparent">
                        <StackPanel Orientation="Vertical">
                            <Border Background="#ED9D2E" Height="80" CornerRadius="20" BorderThickness="0" BorderBrush="Transparent">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="Id" Style="{StaticResource ParamTB}"/>
                                        <TextBlock Text="User" Style="{StaticResource ParamTB}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="1" Background="White"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Border>
                    <Border Margin="0,60,0,0">
                        <DataGrid 
                           ItemsSource="{Binding Users}"
                           AutoGenerateColumns="False"
                           Background="Transparent"
                           GridLinesVisibility="None" 
                           HorizontalAlignment="Stretch"
                           HeadersVisibility="Column"
                           HorizontalGridLinesBrush="Transparent" 
                           VerticalGridLinesBrush="Transparent"
                           BorderThickness="0"
                           CanUserAddRows="False"
                           PreviewMouseLeftButtonDown="DataGrid_PreviewMouseLeftButtonDown"
                           >    
                        <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="BorderThickness" Value="0" />
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="Padding" Value="5" />
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="Foreground" Value="Black" />
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    <Setter Property="VerticalContentAlignment" Value="Center" />
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.Columns>
                                <DataGridTemplateColumn  >
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Background="Transparent" Text="{Binding Id}" Style="{StaticResource ParamTB}" Foreground="Black" VerticalAlignment="Center" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn >
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Margin="0, 5">
                                                <TextBlock Text="{Binding Login}" Style="{StaticResource ParamTB}" Foreground="Black" />
                                                <!--<Border HorizontalAlignment="Left" CornerRadius="20" Width="20" Height="20" Background="LightGray" VerticalAlignment="Center" >
                                                    <Border.Clip>
                                                        <RectangleGeometry Rect="0,0,20,20" RadiusX="10" RadiusY="10"/>
                                                    </Border.Clip>
                                                    <Grid>
                                                        <Image Source="../../../../res/images/bg1.jpg" Stretch="Fill"/>
                                                    </Grid>
                                                </Border>-->
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Width="*"/>
                        </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
        </Grid>
    </Grid>
</UserControl>
