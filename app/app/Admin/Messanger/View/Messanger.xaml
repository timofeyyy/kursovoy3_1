<UserControl x:Class="app.app.Admin.Messanger.View.Messanger"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:app.app.Admin.Messanger.View"
             xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15*"/>
                    <ColumnDefinition Width="2.5*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="15*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Style="{StaticResource EntryTB}" Text="Messanger" HorizontalAlignment="Left" FontWeight="Medium" Grid.Column="1" Grid.Row="0" />

            <Border Style="{StaticResource ParamBorder}" Background="#d20b62"  Grid.Column="1" Grid.Row="1" VerticalAlignment="Stretch"/>
            <!--<TextBox Style="{StaticResource ParamBorderTB}" Text="{Binding Review.Message}" Foreground="Black" FontWeight="Medium" />-->

            <Grid Grid.Column="1" Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="9*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                    <ScrollViewer HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" Margin="10,20">
                        <ItemsControl ItemsSource="{Binding Messages}" >

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource ParamBorder}"  Background="#5a4bb3" Width="200" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="5" >
                                        <TextBlock Style="{StaticResource Value}" Padding="20,15" TextWrapping="Wrap" Text="{Binding Message}" Foreground="White" FontWeight="Medium" />
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
                <Border Style="{StaticResource ParamBorder}"  Grid.Row="1" VerticalAlignment="Stretch" Background="#9c9c9c" Margin="5">
                    <DockPanel>
                        <TextBox Style="{StaticResource ParamBorderTB}" Text="{Binding Message}" Foreground="White" FontWeight="Medium" Width="350" VerticalAlignment="Center" />
                            <Grid HorizontalAlignment="Right"  Margin="0,5,10,5">
                                <Image Width="25"  Source="{svg:SvgImage ../../../../res/icons/send.svg}"/>
                                <Button Style="{StaticResource NoHoverButtonStyle}" Cursor="Pen" Command="{Binding SendCommand}"/>
                            </Grid>
                        </DockPanel>
                    </Border>
                </Grid>
            </Grid>
    </Grid>
</UserControl>
