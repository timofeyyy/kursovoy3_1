<UserControl x:Class="app.app.Admin.Messangers.View.Messangers"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
             xmlns:local="clr-namespace:app.app.Admin.Messangers.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="0.2*"/>
                <RowDefinition Height="11*"/>
                <RowDefinition Height="0.5*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.75*"/>
                <ColumnDefinition Width="10*"/>
                <ColumnDefinition Width="1.75*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="Messangers" Style="{StaticResource EntryTB}" HorizontalAlignment="Left"/>

            <Grid Grid.Row="1" Grid.Column="1">
                <DockPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Border BorderThickness="{Binding NewMessangersHighlight}" BorderBrush="White" Margin="0,0,10,0" >
                            <Button Content="New" Padding="10,0,10,0" Style="{StaticResource NoHoverButtonStyle}" Command="{Binding SelectNewMessangersCommand}" />
                        </Border>
                        <Border BorderThickness="{Binding ExistedMessangersHighlight}" BorderBrush="White" >
                            <Button Content="Existed" Padding="10,0,10,0"  Style="{StaticResource NoHoverButtonStyle}" Command="{Binding SetectExistedMessangersCommand}" />
                        </Border>
                    </StackPanel>
                   
                </DockPanel>
            </Grid>
            <Grid Grid.Row="3" Grid.Column="1">

                <Border Margin="0,0,0,0" Background="#3f4347" CornerRadius="20">
                    <ScrollViewer HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" Margin="10,20">
                        <ItemsControl x:Name="Chats" ItemsSource="{Binding Chats}" >

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="white" BorderThickness="0,1">
                                        <Grid  Margin="0,10">
                                            <StackPanel Orientation="Horizontal">
                                                <Border  Width="40" Height="40" >
                                                    <Border.Clip>
                                                        <RectangleGeometry Rect="0,0,40,40" RadiusX="20" RadiusY="20"/>
                                                    </Border.Clip>
                                                    <Grid>
                                                        <Image Source="{Binding User.UserImage}" Stretch="Fill"/>
                                                    </Grid>
                                                </Border>
                                                <TextBlock Style="{StaticResource ParamTB}" VerticalAlignment="Center" FontSize="15" Text="{Binding User.Login}" Margin="10,0,0,0" />
                                                <TextBlock Style="{StaticResource Value}" VerticalAlignment="Center" FontSize="15" Text="{Binding LastMessage}" Margin="40,0,0,0" />

                                            </StackPanel>
                                            <Button Style="{StaticResource NoHoverButtonStyle}"   
                                                    Command="{Binding ElementName=Chats,  Path=DataContext.OpenChatCommand}"
                                                  CommandParameter="{Binding}"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    <!--<DataGrid 
                        ItemsSource="{Binding Users}"
                        AutoGenerateColumns="False"
                        Background="Transparent"
                        GridLinesVisibility="None" 
                        HorizontalAlignment="Stretch"
                        HeadersVisibility="Column"
                        HorizontalGridLinesBrush="Transparent" 
                        VerticalGridLinesBrush="Transparent"
                        BorderThickness="0"
                        CanUserAddRows="False"
                        PreviewMouseLeftButtonDown="DataGrid_PreviewMouseLeftButtonDown"
                        >
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="Padding" Value="5" />
                            </Style>
                        </DataGrid.CellStyle>
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="Foreground" Value="Black" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Center" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn  >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Background="Transparent" Text="{Binding Id}" Style="{StaticResource ParamTB}" Foreground="Black" VerticalAlignment="Center" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Margin="0, 5">
                                            <TextBlock Text="{Binding Login}" Style="{StaticResource ParamTB}" Foreground="Black" />
                                            --><!--<Border HorizontalAlignment="Left" CornerRadius="20" Width="20" Height="20" Background="LightGray" VerticalAlignment="Center" >
                                                 <Border.Clip>
                                                     <RectangleGeometry Rect="0,0,20,20" RadiusX="10" RadiusY="10"/>
                                                 </Border.Clip>
                                                 <Grid>
                                                     <Image Source="../../../../res/images/bg1.jpg" Stretch="Fill"/>
                                                 </Grid>
                                             </Border>--><!--
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>-->
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
