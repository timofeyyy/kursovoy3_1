<Window x:Class="app.Main.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                            xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
        mc:Ignorable="d"
        Title="MainWindow" Height="550" Width="800"
        MaxHeight="550" MaxWidth="800"
        WindowStyle="None"
        ResizeMode="NoResize"
        xmlns:view="clr-namespace:app.app.Login.View"
        xmlns:vm="clr-namespace:app.app.Main.ViewModel"
           AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        >
    <Grid>
        <Border
            CornerRadius="20" />
        <ContentControl x:Name="Content" Content="{Binding CurrentView}" Margin="0"/>
        <Border Name="ControlPanel" Height="25" VerticalAlignment="Top" Background="Transparent" MouseDown="ControlPanel_MouseDown">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                <Border x:Name="HideApp"  Width="15"
                     Style="{StaticResource ExitApp}" 
                        MouseDown="HideApp_MouseDown"
                        >
                    <Image Source="{svg:SvgImage ../../../res/icons/dash.svg}" Width="10" Height="10" />
                </Border>
                <Border x:Name="CloseApp1"
                    Style="{StaticResource ExitApp}"
                        MouseDown="CloseApp_MouseDown"
                        >

                    <Image Source="{svg:SvgImage ../../../res/icons/close.svg}" Width="15" Height="10" />

                </Border>
            </StackPanel>
        </Border>


    </Grid>

</Window>
